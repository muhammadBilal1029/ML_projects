# 🌸 Iris Flower Predictor App

A simple Streamlit app that uses a trained ML model to predict the species of an iris flower.

## Run Locally
```bash
pip install -r requirements.txt
streamlit run iris_app.py
